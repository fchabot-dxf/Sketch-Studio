// MOVED → #app/ui/debug-panel.js (WAVE A). Re-export shim at OLD path for unmoved importer(s) (main.js).
// Removed in the rewire/cleanup slice. shell→shell via #app/ (never core→apps).
export * from '#app/ui/debug-panel.js';
